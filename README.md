# Honey Badger Swarm
A defensive, self-auditing, secure-by-design software generator powered by AI agents.

## How to use
1. Install requirements: `pip install -r requirements.txt`
2. Run the app: `streamlit run app.py`
3. Enter your Groq API keys in the sidebar and provide your project requirements.
